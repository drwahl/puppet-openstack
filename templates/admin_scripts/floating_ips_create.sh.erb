#!/bin/bash
# THIS FILE IS MANAGED BY PUPPET
# <%= file.split(File::SEPARATOR)[5..-1].join(File::SEPARATOR) %>

# $1 - external network name
# $2 - floating ips start
# $3 - floating ips end
# $4 - floating ips gateway
# $5 - floating ips cidr
# $6 - floating ips name

export OS_TENANT_NAME=<%= admin_tenant_name %>
export OS_USERNAME=<%= admin_user %>
export OS_PASSWORD=<%= admin_password %>
export OS_AUTH_URL="http://<%= auth_host %>:5000/v2.0/"
export SERVICE_ENDPOINT="http://<%= auth_host %>:35357/v2.0"
export SERVICE_TOKEN=<%= admin_password %>

/usr/bin/quantum subnet-create $1 --allocation-pool start=$2,end=$3 --gateway $4 $5 -- --enable_dhcp=False --name $6
